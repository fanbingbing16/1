export enum UserChildrenRole {
  ADMIN = "admin",
  USER = "userChildren",
}

export enum UserChildrenBelong {
  SYSTEM = 1,
  WXAPP = 2,
}

export enum UserChildrenStatus {
  NOTACTIVE = 0,
  ACTIVE = 1,
  DISABLED = 2,
}
