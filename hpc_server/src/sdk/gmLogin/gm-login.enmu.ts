export enum GmLoginRole {
  ADMIN = "admin",
  USER = "gmLogin",
}

export enum GmLoginBelong {
  SYSTEM = 1,
  WXAPP = 2,
}

export enum GmLoginStatus {
  NOTACTIVE = 0,
  ACTIVE = 1,
  DISABLED = 2,
}
