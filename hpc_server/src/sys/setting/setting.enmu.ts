export enum SettingRole {
  ADMIN = "admin",
  USER = "setting",
}

export enum SettingBelong {
  SYSTEM = 1,
  WXAPP = 2,
}

export enum SettingStatus {
  NOTACTIVE = 0,
  ACTIVE = 1,
  DISABLED = 2,
}
